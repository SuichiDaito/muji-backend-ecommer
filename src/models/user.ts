class User {
    constructor(
        id: number,
        username: string,
        password: string,
        status: number,
        id_roles: number,
        token: string
    ) { }
}

export default User;